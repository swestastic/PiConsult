# PiConsult Modes

This folder contains subfolders relating to each mode on the device (Data Stream, Active Test, DTC, Digital Register, and Settings). Each folder contains `ui.pi` and `protocol.py`. `ui.py` contains elements for the display, such as rendering text, gauges, menus, etc. `protocol.py` contains code relevant to sending, receiving, or parsing ECU data.
